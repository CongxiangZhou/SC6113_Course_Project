# SC6113 Course Project

A simple Flask web app with HTML and CSS.

## Run

With Python installed, run these commands in the project folder:

```bash
pip install -r requirements.txt
python app.py
```

